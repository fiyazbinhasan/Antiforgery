@model MVC.XSRF.Models.Transaction
@{
    ViewBag.Title = "WHB - What Happened Bank!?!?!";

}
<div class="container">
    <div class="row">
        <h1 class="text-center text-danger">
            <strong>WHB</strong>
        </h1>
        <h3 class="text-center text-danger">What Happened Bank!?!?!</h3>
        <div class="text-center">
            <em>"you wouldn't know what just happened"</em>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-success">
                <div class="panel-heading">Debit/Credit on Account 1234</div>
                <div class="panel-body">
                    @*@using (Html.BeginForm("Add", "Transaction"))
                    {
                        @Html.AntiForgeryToken()
                        <div class="form-group">
                            <label>Amount (in dollars)</label>
                            <div class="input-group">
                                <span class="input-group-addon">$</span>
                                @Html.TextBoxFor(model => model.TransactionAmount, null, new {@class = "form-control", placeholder = @Html.DisplayNameFor(m => m.TransactionAmount)})
                                <span class="input-group-addon">.00</span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Debit/Credit</label>
                            @Html.DropDownListFor(model => model.TransactionType, new List<SelectListItem> {new SelectListItem {Text = "Debit", Value = "Debit"}, new SelectListItem {Text = "Credit", Value = "Credit"}}, "Select Transaction Type", new {@class = "form-control"})
                        </div>
                        <input type="submit" class="btn btn-warning" value="Make Transaction"/>
                    }*@
                    <form asp-controller="Transaction" asp-action="Add">
                        <div class="form-group">
                            <label>Amount (in dollars)</label>
                            <div class="input-group">
                                <span class="input-group-addon">$</span>
                                <input asp-for="TransactionAmount" class="form-control" placeholder="Amount" />
                                <span class="input-group-addon">.00</span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Debit/Credit</label>
                            <select class="form-control" asp-for="TransactionType">
                                <option>Select Transaction Type</option>
                                <option value="Debit">Debit</option>
                                <option value="Credit">Credit</option>
                            </select>
                        </div>
                        <input type="submit" class="btn btn-warning" value="Make Transaction" />
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>